# Pokédex phase — handoff pointer

The self-contained implementation plan for the scraper-side work (D-106) is
`docs/superpowers/plans/2026-08-14-pokedex-phase.md`; it travels together with the approved
design at `docs/superpowers/specs/2026-08-14-pokedex-phase-design.md`. Those two files are
everything the `../PokemonInvestBatch` context needs — this pointer exists only so the handoffs
directory records that the brief for this phase is the plan itself.
